/**
 * Reducers barrel export
 */

export { panelsReducer, initialPanelsState } from './panelsReducer';
export { treeReducer, initialTreeState } from './treeReducer';
export { canvasReducer, initialCanvasState } from './canvasReducer';
